[buildout]
extends =
    buildout.cfg
    http://jyuplone.cc.jyu.fi/dist/jenkins/kopla.cfg
versions = versions
always-checkout = true

jenkins-test-eggs = collective.roster [test]
jenkins-test-directories = src/collective/roster

[versions]
zc.buildout = 1.6.3
distribute = 0.6.28

[jenkins-test]
recipe = corejet.recipe.testrunner
defaults = ['--auto-color', '--auto-progress', '--xml', '--corejet=pivotal']
